{
  "name": "@tappy/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "command -v forge >/dev/null || { echo \"forge not found. Run ./scripts/setup.sh, or install manually: curl -L https://foundry.paradigm.xyz | bash && foundryup   (foundryup prints the bin path to add to PATH; it is ~/.foundry/bin, or ~/.config/.foundry/bin when XDG_CONFIG_HOME is set)\"; exit 1; }; forge build && node scripts/export-abi.mjs",
    "test": "command -v forge >/dev/null || { echo \"forge not found. Run ./scripts/setup.sh, or install manually: curl -L https://foundry.paradigm.xyz | bash && foundryup   (foundryup prints the bin path to add to PATH; it is ~/.foundry/bin, or ~/.config/.foundry/bin when XDG_CONFIG_HOME is set)\"; exit 1; }; forge test -vv",
    "fmt": "forge fmt",
    "deploy:sepolia": "forge script script/Deploy.s.sol --rpc-url $SEPOLIA_RPC_URL --broadcast",
    "export-abi": "node scripts/export-abi.mjs",
    "typecheck": "echo 'contracts: solidity, see pnpm contracts:test'"
  },
  "main": "./src-ts/index.ts",
  "types": "./src-ts/index.ts",
  "exports": {
    ".": "./src-ts/index.ts",
    "./abi/*": "./abi/*"
  },
  "devDependencies": {
    "@noble/curves": "^2.4.0",
    "@noble/hashes": "^2.4.0",
    "@openzeppelin/contracts": "^5.1.0",
    "tsx": "^4.23.13",
    "viem": "^2.56.3"
  },
  "dependencies": {
    "@tappy/protocol": "workspace:*"
  }
}
